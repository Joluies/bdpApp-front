# NPM Configuration for Vercel and CI/CD
legacy-peer-deps=true
strict-peer-deps=false

# Use exact versions in lockfile
save-exact=false

# Registry
registry=https://registry.npmjs.org/

# Auth
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
